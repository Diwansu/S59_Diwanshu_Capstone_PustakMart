import { Container } from '@mui/material';
import CartList from '../components/cart/CartList';

const Cart = () => {
  return (
    <Container>
      <CartList />
    </Container>
  );
};

export default Cart;
